The development version of the R package "VimCom" is available at github. 
To download and build it do the following in a terminal emulator:

git clone git://github.com/jalvesaq/VimCom.git
R CMD build VimCom
R CMD INSTALL vimcom_0.9-1.tar.gz


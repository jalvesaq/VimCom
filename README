This is the development version of the R package "VimCom".

To download and build the package do the following in a terminal emulator:

git clone git://github.com/jalvesaq/VimCom.git
R CMD build VimCom
R CMD INSTALL vimcom_0.9-1.tar.gz

